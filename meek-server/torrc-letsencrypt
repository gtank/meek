# Minimal tor config for a private meek bridge
# For futher discussion of ORPort settings, see
# https://trac.torproject.org/projects/tor/ticket/7349

SOCKSPort 0               # no local SOCKS proxy
ORPort auto               # hopefully less obvious than ORPort 9001
ExtORPort auto            # configure ExtORPort for PT
ExitPolicy reject *:*     # no exits allowed
BridgeRelay 1             # relay won't show up in the public consensus
PublishServerDescriptor 0 # don't publish to the bridge authority

# use meek-server with Let's Encrypt on port 7002
ServerTransportPlugin meek exec /usr/local/bin/meek-server --log /var/lib/tor/meek.log --port 7002 --lets-encrypt --lets-encrypt-cache /var/lib/tor/meek.cache --lets-encrypt-hosts YOUR-DOMAIN-HERE
